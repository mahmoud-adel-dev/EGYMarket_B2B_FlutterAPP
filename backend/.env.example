# Copy to .env.local for local development. Never commit real credentials.
MONGODB_URI=mongodb+srv://memoadeldev_db_user:R0Oxo4PSIk5yIzVn@cluster0.nlycilg.mongodb.net/b2bmar
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000
APP_ORIGIN=http://localhost:8080
# Optional comma-separated extra first-party origins. In development only,
# loopback origins on random Flutter Web ports are accepted automatically.
APP_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Required for real media uploads. The API refuses fake URLs when these are absent.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional in local development; required before production email verification is enabled.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Seals B2B <no-reply@example.com>"

# Never set true in production.
ALLOW_UNVERIFIED_LOGIN=false
LOG_LEVEL=info
